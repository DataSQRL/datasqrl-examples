{
  "version": "1",
  "enabled-engines": ["vertx", "postgres", "kafka", "flink"],
  "script": {
    "main": "monitoring_agent.sqrl",
    "shared": {
      "data-catalog": {
        "path": "../data-catalog"
      }
    },
    "config": {
      "variant": "test"
    }
  },
  "engines": {
    "flink" : {
      "config" : {
        "table.exec.source.idle-timeout": "1 s"
      }
    }
  }
}
