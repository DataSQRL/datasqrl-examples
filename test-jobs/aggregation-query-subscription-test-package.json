{
  "version": "1",
  "enabled-engines": ["vertx", "postgres", "flink", "kafka"],
  "script": {
    "main": "aggregation-query-subscription-test.sqrl"
  },
  "engines": {
    "flink" : {
      "config" : {
        "table.exec.source.idle-timeout": "1 s"
      }
    }
  }
}
