{
  "version": "1",
  "enabled-engines": ["flink"],
  "script": {
    "main": "aggregation-test.sqrl"
  },
  "engines": {
    "flink" : {
      "config" : {
        "table.exec.source.idle-timeout": "1 s"
      }
    }
  }
}
