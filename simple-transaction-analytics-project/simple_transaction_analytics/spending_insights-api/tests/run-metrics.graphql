query TestRunMetrics {
  RunMetrics(limit: 5) {
    runDate
    windowStart
    windowEnd
    recomputedDayCount
    recomputedMonths
    transactionCountProcessed
    lateTransactionCount
    missingFxRateTxnCount
  }
}
