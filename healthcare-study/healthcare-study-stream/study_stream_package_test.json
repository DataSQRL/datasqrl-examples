{
  "version": "1",
  "enabled-engines": ["kafka", "flink"],
  "script": {
    "main": "study_stream.sqrl",
    "shared": {
      "data-catalog": {
        "path": "../data-catalog"
      }
    },
    "config": {
      "variant": "testdata"
    }
  },
  "engines": {
    "flink" : {
      "config" : {
        "table.exec.source.idle-timeout": "1 s"
      }
    }
  },
  "test-runner": {
    "delay-sec": 120
  }
}
