{
  "version": "1",
  "script": {
    "main": "study_api.sqrl",
    "config": {
      "variant": "api"
    }
  },
  "engines": {
    "flink" : {
      "config" : {
        "table.exec.source.idle-timeout": "1 s"
      }
    }
  }
}
