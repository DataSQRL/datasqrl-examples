{
  "version": "1",
  "enabled-engines": ["vertx", "postgres", "kafka", "flink"],
  "script": {
    "main": "sensors.sqrl"
  },
  "engines": {
    "flink" : {
      "config" : {
        "table.exec.source.idle-timeout": "1 s"
      }
    }
  },
  "dependencies": [{
    "sensor-data": {
      "name": "sensor-testdata",
      "version": "1",
      "variant": "dev"
    }
  }]
}